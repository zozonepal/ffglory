{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.email === 'deepsonpokhrel12@gmail.com')",
        ".write": "auth != null && (auth.uid === $uid || auth.token.email === 'deepsonpokhrel12@gmail.com')"
      }
    },
    "payment_requests": {
      ".read": "auth != null && auth.token.email === 'deepsonpokhrel12@gmail.com'",
      "$req_id": {
        ".write": "auth != null && ((!data.exists() && newData.child('userId').val() === auth.uid) || auth.token.email === 'deepsonpokhrel12@gmail.com')"
      }
    },
    "settings": {
      "payment_qr": {
        ".read": "auth != null",
        ".write": "auth != null && auth.token.email === 'deepsonpokhrel12@gmail.com'"
      }
    }
  }
}
